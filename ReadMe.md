#Framework
- I did not install framework since my preffered framework, React, could not be supported since node.js was not installed in the PC

#Database
```bash
# Install mysql driver in terminal

```